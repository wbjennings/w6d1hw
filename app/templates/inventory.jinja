{% extends 'base.jinja '%}

{% block content %}
    <h2>Inventory</h2>
    <form action="" method="post">
    {{ form.hidden_tag() }}
        <p>
            {{ form.body.label }}<br>
            {{ form.body(size=32) }}
        </p>
        <p>
            {{ form.submit() }}
        </p>
</form>
{% endblock content %}

